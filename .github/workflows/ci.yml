name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # --max-warnings=0 so regressions like render-blocking font tags fail the
      # build instead of landing as warnings nobody reads.
      - run: npx eslint --max-warnings=0

      - run: npx tsc --noEmit

      - run: npm run build
